[package]
name = "kataglyphis_gui"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = []
gui = ["dep:gstreamer"]
gui_unix = ["gui", "dep:gtk"]
gui_linux = ["gui", "_gui_wgpu_deps"]
gui_wgpu = ["gui_windows"]
gui_windows = ["gui", "_gui_wgpu_deps"]
_gui_wgpu_deps = [
    "dep:gstreamer-app",
    "dep:kataglyphis_media",
    "kataglyphis_media/gstreamer",
    "dep:winit",
    "dep:wgpu",
    "dep:pollster",
    "dep:bytemuck",
    "dep:egui",
    "dep:egui-winit",
    "dep:egui-wgpu",
]
onnx_tract = ["kataglyphis_inference/onnx_tract"]
onnxruntime = ["kataglyphis_inference/onnxruntime"]

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = { workspace = true }
log = { workspace = true }
kataglyphis_core = { path = "../core" }
kataglyphis_inference = { path = "../inference", optional = true }
# GStreamer's init, which points GST_PLUGIN_PATH at the plugins a package carries
# beside the exe; the same entry point the webcam engine uses.
kataglyphis_media = { path = "../media", optional = true }
kataglyphis_telemetry = { path = "../telemetry" }
tokio = { version = "1", features = ["sync", "time"] }
chrono = { workspace = true }

gtk = { version = "0.11", package = "gtk4", features = ["v4_10"], optional = true }
# Workspace-pinned, NOT a local version: these used to be hard-coded at 0.25
# here while crates/media followed the workspace pin at 0.24, so a build with
# both features linked two GStreamer major versions - two -sys layers binding
# the same system libgstreamer.
gstreamer = { workspace = true, optional = true }
gstreamer-app = { workspace = true, optional = true }
winit = { version = "0.30", optional = true }
wgpu = { version = "30", optional = true }
pollster = { version = "1.0", optional = true }
bytemuck = { version = "1.25", features = ["derive"], optional = true }
egui = { version = "0.36", optional = true }
egui-winit = { version = "0.36", optional = true }
egui-wgpu = { version = "0.36", optional = true }
sysinfo = { workspace = true }

[lints]
workspace = true
